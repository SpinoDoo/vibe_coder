# Copy this file to .env and fill in your key. .env is not committed to git
# (see .gitignore) and is never read by anything except run.py at startup.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the default model used for proposing/generating changes.
# ENGINE_MODEL=claude-sonnet-5
